
class App {


    constructor(initData) {

        this._store = new Store();
        this._initData(initData);

        // lol kek cheburek
        debugger;
    }

    _initData(data) {
        data.forEach((record) => {
            this._state
        });
    }

    run(initData) {
        console.log('Hi');
        this._initData(initData);
    }
    
}